import React from 'react'

function SideBar() {
    return (
        <div className='sidebar' style={{ height: window.innerHeight - 50 }}>
            sidebar
        </div>
    )
}

export default SideBar